# Design docs


### استفاده از معماری ها 

1. Adapters/Strategy/Factory
    - استفاده از یک interface جهت یکسان ساری توابع بخش پرداخت و وریفای پرداخت 
    - برای اضافه کردن درگاه کافیست سرویس آداپتر مربوطه و معرفی سرویس در config/settings.php انجام شود
    - بدلیل اینکه برای اتصال به درگاه لازم بود تا با یک سرویس خارجی اتصال برقرار شود، به عنوان آداپتر معرفی گردید تا از بخش سرویس های مدیریت مبلغ پرداخت و شکستن پرداخت ها جدا شود.
    - نحوه فراخوانی درگاه انتخابی هم با استفاده از return new match قابل مدیریت بود هم با استفاده از config که بنظرم بخش config عمومی تر و برای معرفی سرویس هر درگاه استاندارد تر بود.
    - تمام این بخش بدون استفاده از AI نوشته شده است.

2. Repository
    - برای ارتباطات با دیتابس جهت نوشتن و حذف کردن و خواندن از ریپازیتوری استفاده شده است
    - برخی از بخش ها بصورت استثنا از ریپازیتوری استفاده نشده است. برای مثال بخش تراکنش ها با استفاده از بیلدر پیاده سازی شده است
   - تمام این بخش بدون استفاده از AI نوشته شده است.

3. Builder
    - برای ساخت و بروزرسانی تراکنش ها از بیلدر مروبطه استفاده میشود و با توجه به وضعیت تراکنش و مبلغ باقیمانده پرداخت نیز عملکرد متفاوتی را معرفی میکند. 
    - این بخش برای جلوگیری از تکرار کد ها ایجاد شد. 
    - برای بخش تغییر وضعیت ها از دیزاین پترن های state و ساختار سرویس های مختلف به ازای هر سرویس میشد استفاده کرد که بدلیل نداشتن اطلاعات کافی در این خصوص و عدم استفاده از AI برای ساختارمندی کد، تصمیم به استفاده از بیلدر برای مدیریت وضعیت تراکنش ها و تغییر وضعیت درخواست گرفتم.
   - تمام این بخش بدون استفاده از AI نوشته شده است.

4. Response Service
    - برای ساختار مند بودن خروجی api ها این سرویس اضافه شد 
    - جهت بهینه سازی بهتر است هم این سرویس و هم برای خروجی سرویس آداپتر های درگاه نیز Response Builder ایجاد شود تا خروجی ساختارمند تر و دققتر باشد. 
   - تمام این بخش بدون استفاده از AI نوشته شده است.

5. ArrayCrypt
   - بدلیل امکان افزودن درگاه از طریق پنل مدیریت، کلید های مهم در این بخش امکان ذخیره شدن دارند.
   - اما برای ارتقا امنیت مقادیر بصورت دیکریئت ذخیره میشوند و با استفاده از این کلاس قابل مشاهده خواهند بود.
   - بدلیل مهم بودن اطلاعات اما ترجیح داده میشود که در فایل env این مقادیر ذخیره شده و از دیتابیس حذف شود.

6. Swagger Docs
    - بخش داکیومنت API با استفاده از AI توسعه و نوشته شد. 
    - داکیومنت ها با فرمت postman نیز در پوشه Database نیز وجود دارند.

7. Event broadcasting Channel
    - سمت فرانت با توجه به شماره سفارش مربوطه به چنل مربوطه گوش میدهد تا پس از تغییر وضعیت تراکنش و درخواست از آن مطلع شود.
    - نیاز است تنظیمات سرور پوش نوتیفیکیشن Reverb یاPusher نیز اعمال گردد که درحال حاضر در پروژه نصب و راه اندازی نشده است.
   - این بخش با کمک داکیومنت لاراول نوشته شده است.و فقط برای خطای بخش AppServiceProvider از AI کمک گرفته شد.

8. Test files
    - بخش تست نویسی با کمک chatgpt توسعه یافت.
    - کد بصورت کامل از AI دریافت نشده با توجه به توضیحات و بخش بخش شدن کد و با استفاده از داکیونت توسعه یافته است
